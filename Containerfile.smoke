FROM rust:1.82-bookworm

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        ca-certificates \
        curl \
        git \
    && rm -rf /var/lib/apt/lists/*

ENV PATH=/usr/local/cargo/bin:/usr/local/rustup/bin:$PATH

WORKDIR /workspace/relay
CMD ["sleep", "infinity"]
